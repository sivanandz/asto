## 2025-02-28 - Add Tooltips to IconButtons
**Learning:** While adding tooltips is a straightforward accessibility win, it is important to remember that they serve a dual purpose: providing screen reader accessible labels and visual hover context. This micro-UX change was specifically added for the `IconButton` in the search field and settings screen.
**Action:** Always ensure `IconButton` widgets include a `tooltip` attribute.
