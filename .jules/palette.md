## 2024-09-03 - Added Tooltips to IconButtons

**Learning:** Missing tooltips on `IconButton` widgets reduce accessibility for screen readers and users who rely on hover hints for icon-only buttons.
**Action:** Always ensure `tooltip` properties are explicitly defined for all `IconButton`s that don't contain descriptive text.
